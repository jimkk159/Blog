name: backend code unit_test

on: [pull_request, push]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: npm install
        run: cd backend
      - name: npm install
        run: npm ci
      - name: test
        run: npm test